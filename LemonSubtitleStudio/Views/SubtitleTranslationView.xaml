<UserControl x:Class="LemonSubtitleStudio.Views.SubtitleTranslationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#111125">
    <Grid Background="#111125">
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="36" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#111125" BorderBrush="#534433" BorderThickness="0,0,0,1" Padding="16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Subtitle Translation" FontSize="20" FontWeight="Bold" Foreground="#FFB95C" Margin="0,0,12,0" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Export All" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding ExportAllCommand}" Margin="0,0,8,0" Padding="12,6" />
                    <Button Content="Translate" Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding StartProcessingCommand}" Padding="16,6" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#1A1A2E" BorderBrush="#534433" BorderThickness="0,0,0,1" Padding="16,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="TRANSLATION MODEL" Style="{StaticResource LabelCapsStyle}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding TranslationModels}" SelectedItem="{Binding SelectedTranslationModel, Mode=TwoWay}" Width="160" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="ENGINE" Style="{StaticResource LabelCapsStyle}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding TranslationEngines}" SelectedItem="{Binding TranslationEngine, Mode=TwoWay}" Width="100" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="SOURCE" Style="{StaticResource LabelCapsStyle}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SourceLanguage, Mode=TwoWay}" Width="120" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="TARGET" Style="{StaticResource LabelCapsStyle}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding TargetLanguage, Mode=TwoWay}" Width="120" Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="OUTPUT DIRECTORY" Style="{StaticResource LabelCapsStyle}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <Border Background="#333348" CornerRadius="4" Padding="8,4" BorderBrush="#534433" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding OutputDirectory}" Foreground="#D9C3AC" FontSize="11" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                            <Button Grid.Column="1" Background="Transparent" BorderThickness="0" Command="{Binding BrowseOutputDirectoryCommand}" Cursor="Hand" Padding="4,0">
                                <materialDesign:PackIcon Kind="FolderOpen" Width="14" Height="14" Foreground="#FFA500" />
                            </Button>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: File Queue -->
            <Border Grid.Column="0" Background="#1E1E32" BorderBrush="#534433" BorderThickness="1" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Margin="8" Padding="24,16" Background="#0C0C1F" BorderBrush="#534433" BorderThickness="1"
                            AllowDrop="True" Drop="DropHandler" Cursor="Hand" CornerRadius="8">
                        <Border.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding SelectFileCommand}" />
                        </Border.InputBindings>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="CloudUpload" Width="32" Height="32" Foreground="#D9C3AC" Margin="0,0,0,8" HorizontalAlignment="Center" />
                            <TextBlock Text="Drop subtitle files here" Foreground="#D9C3AC" FontSize="14" HorizontalAlignment="Center" />
                            <TextBlock Text="SRT, VTT, ASS" Foreground="#606070" FontSize="11" HorizontalAlignment="Center" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="4">
                        <ItemsControl ItemsSource="{Binding Tasks}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="4,2" Padding="8,6" Background="#1E1E32" CornerRadius="0" BorderBrush="#2A2A3E" BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="#333348" CornerRadius="8" Margin="0,0,8,0">
                                                <materialDesign:PackIcon Kind="FileDocument" Width="18" Height="18" Foreground="#FFB95C" />
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FileName}" Foreground="#E2E0FC" FontSize="12" FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                    <TextBlock Text="{Binding FileSizeFormatted}" Foreground="#606070" FontSize="10" Margin="0,0,8,0" />
                                                    <TextBlock Text="•" Foreground="#606070" FontSize="10" Margin="0,0,8,0" />
                                                    <TextBlock Text="{Binding NodeCount, StringFormat={}{0} nodes}" Foreground="#606070" FontSize="10" />
                                                </StackPanel>
                                            </StackPanel>
                                            <TextBlock Grid.Column="2" Text="{Binding Progress, StringFormat={}{0}%}" Foreground="#FFB95C" FontSize="11" FontFamily="Consolas" VerticalAlignment="Center" />
                                            <ProgressBar Grid.Row="1" Grid.ColumnSpan="3" Value="{Binding Progress}" Maximum="100" Height="2" Margin="0,4,0,0"
                                                         Background="#333348" Foreground="#FFA500" BorderThickness="0" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="4" Background="Transparent" />

            <!-- Right: Source / Target Split -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="120" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="4" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#1E1E32" BorderBrush="#534433" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Background="#28283D" Padding="12,6" BorderBrush="#534433" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                                <TextBlock Text="{Binding SourceLanguage, StringFormat={}SOURCE ({0})}" Foreground="#D9C3AC" FontSize="10" FontWeight="Bold" />
                            </Border>
                            <ListBox Grid.Row="1" ItemsSource="{Binding SourceSubtitles}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0,0,0,6" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border Margin="0,0,0,6" Padding="12,8" Background="#111125" BorderBrush="#534433" BorderThickness="1" CornerRadius="6">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="Auto" />
                                                            </Grid.RowDefinitions>
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Text="{Binding StartTimeFormatted}" Foreground="#FFB95C" FontSize="10" FontFamily="Consolas" />
                                                                <TextBlock Grid.Column="1" Text="{Binding Index, StringFormat={}#{0}}" Foreground="#D9C3AC" FontSize="10" FontFamily="Consolas" />
                                                            </Grid>
                                                            <TextBlock Grid.Row="1" Text="{Binding OriginalText}" Foreground="#D9C3AC" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                        </Grid>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="4" Background="Transparent" />

                    <Border Grid.Column="2" Background="#1E1E32" BorderBrush="#534433" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Background="#28283D" Padding="12,6" BorderBrush="#534433" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                                <TextBlock Text="{Binding TargetLanguage, StringFormat={}TARGET ({0})}" Foreground="#FFA500" FontSize="10" FontWeight="Bold" />
                            </Border>
                            <ListBox Grid.Row="1" ItemsSource="{Binding TranslatedSubtitles}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0,0,0,6" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border Margin="0,0,0,6" Padding="12,8" Background="#111125" BorderBrush="#534433" BorderThickness="1" CornerRadius="6">
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="Auto" />
                                                            </Grid.RowDefinitions>
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Text="{Binding StartTimeFormatted}" Foreground="#FFB95C" FontSize="10" FontFamily="Consolas" />
                                                                <materialDesign:PackIcon Grid.Column="1" Kind="CheckCircle" Width="14" Height="14" Foreground="#4ADE80" Visibility="{Binding IsTranslated, Converter={StaticResource BoolToVisibilityConverter}}" />
                                                            </Grid>
                                                            <TextBlock Grid.Row="1" Text="{Binding TranslatedText}" Foreground="#FFB95C" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>

                <GridSplitter Grid.Row="1" Height="4" Background="Transparent" />

                <!-- Translation Log -->
                <Border Grid.Row="2" Background="#1E1E32" BorderBrush="#534433" BorderThickness="1" CornerRadius="8" Margin="0,6,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Background="#28283D" Padding="12,4" BorderBrush="#534433" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0">
                            <TextBlock Text="TRANSLATION LOG" Foreground="#D9C3AC" FontSize="10" FontWeight="Bold" />
                        </Border>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="8">
                            <ItemsControl ItemsSource="{Binding Logs}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" Foreground="#D9C3AC" FontSize="11" FontFamily="Consolas" Padding="2" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="3" Background="#1A1A2E" BorderBrush="#534433" BorderThickness="0,1,0,0" Padding="12,0">
            <Grid>
                <Button Content="Clear Queue" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding ClearQueueCommand}" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
